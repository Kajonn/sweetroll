# I4 Character Sheet Frontend Acceptance — 2026-09-06 (recorded 2026-09-07)

Per plan `docs/superpowers/plans/2026-09-06-i4-character-sheet.md` Task 12 and
spec `docs/superpowers/specs/2026-09-06-i4-character-sheet-design.md`
(Verification and Acceptance section).

## Tested commit / tree

- Tested commit: `be69809` (`fix: guard seeder repair against migration references`)
- Tested tree: `87e80e0`
- All verification commands below were run fresh in this session from the
  isolated worktree at `/home/jonas/dev/sweetroll/.worktrees/i4-character-sheet`,
  on commit `be69809`, against the `localhost:5432` PostgreSQL instance.
- Closure commit: the commit carrying this file with subject
  `docs: close I4 character sheet frontend`. Its SHA postdates the test runs;
  it is recorded in `.superpowers/sdd/2026-09-06-i4-character-sheet/task-12-report.md`
  (gitignored working evidence, preserved alongside this record). No code changed
  between `be69809` and the closure commit — closure artifacts only
  (this acceptance doc, `design_v2.md` I4 status, plan + spec status edits).

## Verification commands and counts (all exit 0, unpiped)

| # | Command | Result |
|---|---------|--------|
| 1 | `npm test` | 28 files, **269/269 pass** |
| 2 | `TEST_DATABASE_URL=postgres://sweetroll:sweetroll@localhost:5432/sweetroll npm run test:integration` | 12 files, **122/122 pass** |
| 3 | `npm run typecheck` | pass, exit 0 |
| 4 | `npm run build` | pass, exit 0 |
| 5 | `npm run contracts:check` | pass, exit 0 |
| 6 | `npm run web:test` | 59 files, **484/484 pass** |
| 7 | `npm run web:typecheck` | pass, exit 0 |
| 8 | `npm run web:build` | pass, exit 0 (emits `dist/sw.js`, `dist/offline-manifest.json`) |
| 9 | `DATABASE_URL=postgres://sweetroll:sweetroll@localhost:5432/sweetroll AUTHORITATIVE_ROLL_SECRET=development-only-roll-secret-32-bytes npm run web:test:e2e` | **19/19 pass** (acceptance 2, character sheets 6 = 3 reference systems x 360/1280, smoke 1, visual 10) |
| 10 | `DATABASE_URL=postgres://sweetroll:sweetroll@localhost:5432/sweetroll AUTHORITATIVE_ROLL_SECRET=development-only-roll-secret-32-bytes npm run web:test:offline` | **13/13 pass** (offline acceptance 3 systems + dev-signin absence 1, coordination 2, worker/deep-link 1, privacy 5, update 1) |

## Production asset / offline proof

- `web/dist/offline-manifest.json`: `buildId "acceptance-update-mtr5ibga"`,
  assets `["/index.html", "/assets/index-BVZY22cT.js", "/assets/index-DWFf63OW.css"]`
  (entry HTML + hashed JS/CSS; no sourcemaps, no API/export bodies).
- `web/dist/sw.js` (3.55 kB): asset-only worker. `shouldHandle` returns false for
  `/api*` and `/dev*`; navigation fallback only for known shell routes
  (`/`, `/characters/new`, `/characters/:id` single segment); transactional
  `cache.addAll` install (partial cache never reports ready); no `skipWaiting`;
  obsolete `sweetroll-offline-assets:*` caches purged on activate.
- `web/tests/offline/offline.spec.ts` proves the built worker controls the page
  and a deep link reloads fully offline; `update.spec.ts` proves a real second
  build waits/activates with pending edits, caches stay asset-only
  (no `/api` URLs in CacheStorage), and the queue drains exactly once.
- Screenshots on disk after the runs (`web/test-results/`, timestamps 2026-09-07):
  `offline-d20/pbta/pool-360-pending.png` + `-synced.png` (queue pending vs
  drained after offline close/reopen), `offline-update-360-waiting.png`,
  `offline-update-1280-active.png`. E2E character-sheet tests capture
  `test-results/e2e-character-<system>-<width>.png` per test code during the
  passing 19/19 run; only the later offline run's PNGs remain on disk.
  Committed visual baselines: `web/tests/visual/__screenshots__/visual.spec.ts/`
  (library + document-editor at 360/1280; re-baselined in Task 11 for header drift
  proven pre-existing at base `2738344`).
- Axe: `web/tests/e2e/character-sheet.spec.ts` runs AxeBuilder on every
  reference system at 360 and 1280 px and asserts zero serious/critical
  violations (6/6 green, including the Task 11 PbtA `color-contrast` fix via
  `--color-warning-fg`); `acceptance.spec.ts` asserts the library passes axe.
  No horizontal overflow at either width (scrollWidth <= innerWidth + 1).

## Final DB effects (cumulative test leftovers, same localhost:5432 instance)

After the full block: `characters` 32, `character_command_executions` 56,
`character_activity_events` 56, `idempotency_receipts` 39, `users` 3,
`character_rolls` 0. Browser suites create uniquely-named characters per test
and do not clean them up; each offline acceptance asserts exactly one
`character_resource_bumped` effect (revision +1) including across a
lost-response retry with identical frozen key/body and a server-side replay
asserting `reconciliation.replayed === true` with no second effect.

## Browser support limitations (binding)

- Offline availability is best-effort. Browser storage may be evicted; never
  promise indefinite durability. A character is shown "Available offline" only
  after a compatible projection snapshot is committed AND the controlling worker
  confirms its build cache — readiness is eviction-aware.
- Where Web Locks are unsupported, sheets remain read-only with an explanation;
  no unsafe editing is offered. No timeout-based lock stealing: one
  editing/synchronizing tab per character; takeover quiesces the owner first.
- A never-settling transport is still bounded: Task 6 added a 30 s abort that
  preserves the frozen request bytes and treats timeout as an uncertain outcome
  (immutable retry with the same key), per the uncertain-outcome protocol.
- `SWEETROLL_TEST_AUTH=1` is a test-only escape hatch for the production
  offline fixture (defaults OFF; no production UI affordance). Never set it
  outside local/CI acceptance runs.
- Offline writes are field sets and direct resource bumps only. Actions,
  creation, archive/recovery, migration and authoritative export require
  connectivity. Discarding an uncertain attempted command is not server
  cancellation; expiry (>30 days) requires manual review, never automatic
  new-key retry.

## Requirement reconciliation (every plan requirement -> task/test)

| Requirement (plan/spec) | Task (commit) | Proving test |
|---|---|---|
| Completion projection, no replay mutation (T1) | `7bfc2cc` feat: project omitted required character fields | `src/systems/runtime.test.ts`, `src/transport/http/characters.test.ts`, `tests/integration/characters.test.ts` (completion completionFields assertions; replayed receipts lack the property; resolve spy uncalled) |
| Authorized creation metadata, no writes (T2) | `e31173c` feat: expose authorized character creation options | `tests/integration/character-creation-options.test.ts` (7 tests; row-count stability; unauthorized describe uncalled) |
| Sign-out + character transport (T3) | `54ff80a` + fix `3a782d4` | `src/transport/http/identity.test.ts`, `web/src/characters/api.test.ts` (401/404-purge/409/422 retention, malformed-success uncertainty, cookie flags, same-origin reject) |
| Durable store (T4) | `73c81e6` feat: persist character state and command attempts | `web/src/characters/store.test.ts` (reopen, ordering, rollback, frozen-body immutability, atomic ack, quota, cross-account, tombstone late-ack rejection) |
| Sequential session, immutable attempts (T5) | `6948ec5` + fix `b4dd823` | `web/src/characters/session.test.ts` (crash points, revision sequencing, conflict reapply ordering, expiry, 401/404, storage failure, late responses; identical frozen bodies on retry) |
| Account/tab coordination, 30 s transport bound (T6) | `5453474` + fix `02c782c` | `web/src/characters/identity.test.ts`, `coordination.test.ts`, shell tests; real-contention proof deferred to Task 11 per plan |
| Projection renderer, a11y (T7) | `fa5caca` + fixes `36f40ac`, `34bde15` | `CharacterSheet.test.tsx`, `FieldControl.test.tsx`; e2e `character-sheet.spec.ts` (3 systems x 2 widths, keyboard, axe, overflow); no evaluator/package/router/transport/storage imports in renderer |
| Creation + embeddable routes (T8) | `aa8dc03` + fix `78d92e8` | `CreateCharacter.test.tsx`, `CharacterRoute.test.tsx` (uncertain-create identical body/key retry, double-click suppression, deep-link account isolation) |
| Conflict review + tools (T9) | `e7bc777` + fix `a16530b` | `ConflictReview.test.tsx`, `CharacterTools.test.tsx` (discard/reapply with new keys, export gated on drained queue, archive read-only, migration re-preview, rollback limits) |
| Production offline assets (T10) | `c84d0fb` + fix `2738344` (eviction-aware readiness) | `web/src/offline/*.test.ts`, `offline.spec.ts` smoke; manifest + `sw.js` inspected; `tests/offline/**` excluded from Vitest |
| Real browser/tab acceptance (T11) | `651d67c` + fixes `a2976d6`, `be69809` | `web/tests/offline/` 13 tests + e2e sheets 6 tests (listed above); screenshots + axe; exactly-one-effect DB assertions; I3 revoke-purge and seeder RESTRICT-guard regressions included |
| I3 receipt compatibility (kept explicit) | T1 additive optional `completionFields`; projection `1.0` literals untouched | Historical 30-day create/command receipts without the property replay without re-resolution (`replayed.projection` lacks `completionFields`, `resolveSpy` uncalled); integration `character-http-acceptance.test.ts` 7/7 incl. all three reference systems; full I3 backend suites green in this run (122 integration incl. load p95 within budget) |

I3 receipt compatibility is explicitly covered: the additive optional field does
not change reconciliation literals or ETags, and replay of old receipts never
recomputes outcomes. Existing System Builder suites remain green
(`npm test` 269, `web:test` 484, e2e 19).

## Outcome

Full acceptance + regression pass on tested commit `be69809` (tree `87e80e0`).
**I4 is closed.** I5 is untouched: no library/search, onboarding/profile,
install prompts, manifest/standalone presentation, or Player navigation was added.
