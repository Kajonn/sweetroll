# G0 Reference and Frontend Checks Acceptance — 2026-09-08

Per plan `docs/superpowers/plans/2026-09-08-gui-integration.md` G0
(remaining fixes). G0 base (`docs/ui/visual-reference.md`, CI
web/e2e/offline jobs, real-routed conflict banner) was committed
beforehand (`994edc2`, `b5a2586`). This record covers the remaining
gaps only: the stale synthetic-banner note, `visual.spec.ts` CI
wiring, and the deliberate-failure negative control.

## Tested commit / tree

- Base commit: `a32a214` (`fix(g1): retryable revocation error, clear
  account queries on lifetime change`), branch `fix/g1-lifecycle-repairs`,
  worktree `/home/jonas/dev/sweetroll/.worktrees/g1-lifecycle`.
- Checks below were run in this session against the working tree at
  `a32a214` plus the uncommitted G0 fixes described here (docs + CI
  YAML only — no product logic changes, no route/data changes).
- Closure commit: the commit carrying this file (subject prefix
  `ci(g0):`). No code changed between the test runs and the closure
  commit — closure artifacts only (this file, `docs/ui/visual-reference.md`
  §6 fix, `.github/workflows/ci.yml` e2e run list).

## Changes accepted here

1. `docs/ui/visual-reference.md` §6: the note claiming the
   `visual.spec.ts` conflict banner "is still synthetic
   `page.setContent`" was stale and contradicted §8 and the code. Fixed
   to state it was replaced with the real routed flow in `b5a2586`.
2. `.github/workflows/ci.yml` (`web-e2e` job): added
   `tests/e2e/visual.spec.ts` (10 tests incl. the real 409 conflict
   flow) to the existing run list alongside `smoke.spec.ts`,
   `acceptance.spec.ts`, `character-sheet.spec.ts`. DB/server
   prerequisites unchanged — the visual tests use the same backend as
   the existing routed specs.
3. This acceptance record.

## Verification commands actually run (all in this session)

| # | Command (workdir) | Result |
|---|-------------------|--------|
| 1 | `npx vitest run src/i18n/index.test.ts` (`web/`, with assertion temporarily broken to `LIBRARY-BROKEN-NEGATIVE-CONTROL`) | **1 failed / 2 passed** — `AssertionError: expected 'Library' to be 'LIBRARY-BROKEN-NEGATIVE-CONTROL'`. Deliberate frontend failure fails the check (plan line 67 red-control). |
| 2 | `npx vitest run src/i18n/index.test.ts` (`web/`, after full revert) | **3/3 pass**. Working tree contains no intentional break (`git status` shows only the two G0 files). |
| 3 | `git diff --check` (worktree root) | Clean, exit 0. |
| 4 | `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` | `YAML OK` — CI YAML parses after the run-list edit. |

## Explicitly NOT run / NOT claimed

- Full Playwright e2e was **not** run locally in this session (per task
  constraints). The `visual.spec.ts` CI wiring is validated by YAML
  parse + same-prerequisite inspection only — **no CI green run is
  claimed here**. CI status must be read from the CI system after push.
- Full `npm test` in `web/` was **not** re-run in this session; only the
  `src/i18n/index.test.ts` scope above (plus its negative control).
- No browser/device run in this session, so no new theme, viewport, or
  screenshot evidence is recorded here beyond what is already committed:
  reference-system fixture is the **d20** template clone;
  `conflict-banner-{360,1280}.png` baselines were regenerated under the
  real routed 409 flow in `b5a2586` (reviewed per `visual-reference.md`
  §8 — prior committed evidence, not re-verified here).

## Prior findings carried forward (not re-tested here)

- **Mockup unavailable:** direct fetch of the Tablefolk GUI mockup on
  2026-09-08 returned **HTTP 401**. `docs/ui/reference/` stays empty
  with `.gitkeep`; no mockup screenshots are claimed.
- **360px banner crop:** the committed `conflict-banner-360.png`
  honestly captures a ~605px-wide banner clipped inside the editor pane
  at a 360px viewport (page itself does not scroll). **Not fixed here —
  owned by G3** (view-owned layouts); G1 lifecycle repairs come first
  per plan dependencies.
- `npm run test:integration --no-file-parallelism` and load budgets are
  untouched by this change.

## Remaining limitations

- G0 exit ("green CI includes the frontend") depends on CI executing the
  extended `web-e2e` run list; that green run is still to be observed
  post-push and is not asserted by this file.
- Visual completion is not claimed on screenshot similarity alone (plan
  G0 exit); the baselines remain review artifacts for G1–G4.
